import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import RingMenu from "../components/RingMenu";
import { DarkModeToggle } from "@/components/DarkModeToggle";
import { Providers } from "./Providers";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

const porterSans = localFont({
  src: "./fonts/porter-sans-inline-block.otf",
  variable: "--font-porter-sans",
  weight: "100 900",
});
const porterMono = localFont({
  src: "./fonts/porter-sans-inline-block.ttf",
  variable: "--font-porter-mono",
  weight: "100 900",
});

const poppinsMedium = localFont({
  src: "./fonts/Poppins/Poppins-Medium.ttf",
  variable: "--font-poppins-medium",
  weight: "400",
});

const poppinsRegular = localFont({
  src: "./fonts/Poppins/Poppins-Regular.ttf",
  variable: "--font-poppins-regular",
  weight: "600",
});

const poppinsLight = localFont({
  src: "./fonts/Poppins/Poppins-Light.ttf",
  variable: "--font-poppins-light",
  weight: "100",
}); 

const poppinsBold = localFont({
  src: "./fonts/Poppins/Poppins-Bold.ttf",
  variable: "--font-poppins-bold",
  weight: "800",
});

export const metadata: Metadata = {
  title: "Alejandro?",
  description: "portfolio generated by Alejandro?",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${porterSans.variable} ${porterMono.variable} ${geistMono.variable} ${geistSans.variable} ${poppinsBold.variable} ${poppinsRegular.variable} ${poppinsMedium} ${poppinsLight.variable} antialiased`}
      >
        <Providers>
        <div>
        <DarkModeToggle /> {/* Dark Mode Toggle */}</div>
        <RingMenu /> {/* Floating Sidebar */}
        {children}
        </Providers>
      </body>
    </html>
  );
}
